[workspace]
name = "dl2-head-tracking"
version = "0.1.0"
description = "Head tracking mod for Dying Light 2 using CameraUnlock shared library"
channels = ["conda-forge"]
platforms = ["win-64"]

[tasks]
update-submodule = "git submodule update --remote cameraunlock-core && powershell -NoProfile -ExecutionPolicy Bypass -File cameraunlock-core/scripts/update-submodule.ps1"
sync = "git submodule update --remote cameraunlock-core"
# Setup CMake build (x64 for DL2)
# No -G: let CMake auto-detect the installed Visual Studio generator. Pinning
# "Visual Studio 17 2022" broke on CI once the windows runner image moved to
# VS 2026 (VS 17 absent); auto-detect picks whatever VS the machine has.
setup = { cmd = """
    powershell -Command "if (-not (Test-Path 'build')) { cmake -B build -A x64 } else { Write-Host 'Build directory exists, skipping setup' -ForegroundColor Yellow }"
""" }

# Build debug configuration
build = { cmd = "cmake --build build --config Debug", depends-on = ["setup"] }

# Build release configuration
build-release = { cmd = "cmake --build build --config Release", depends-on = ["setup"] }

# Deploy debug build to game directory
deploy = { cmd = "powershell -ExecutionPolicy Bypass -File scripts/deploy.ps1 Debug", depends-on = ["build"] }

# Deploy release build to game directory
deploy-release = { cmd = "powershell -ExecutionPolicy Bypass -File scripts/deploy-release.ps1", depends-on = ["build-release"] }

# Detect game installation path
detect-game = { cmd = "powershell -ExecutionPolicy Bypass -File scripts/detect-game.ps1" }

# Clean build artifacts
clean = { cmd = """
    powershell -Command "if (Test-Path 'build') { Remove-Item 'build' -Recurse -Force }; Write-Host 'Build artifacts cleaned' -ForegroundColor Green"
""" }

# Full clean including release output
clean-all = { cmd = """
    powershell -Command "if (Test-Path 'build') { Remove-Item 'build' -Recurse -Force }; if (Test-Path 'release') { Remove-Item 'release' -Recurse -Force }; Write-Host 'All build artifacts cleaned' -ForegroundColor Green"
""" }

# Validate release readiness
validate-release = { cmd = "powershell -ExecutionPolicy Bypass -File scripts/validate-release.ps1" }

# Bump vendored mod-loader copies to latest upstream within pinned range.
# Manual; commit the result. CI does not run this.
update-deps = { cmd = "powershell -ExecutionPolicy Bypass -File scripts/update-deps.ps1" }

# The config tests, the config differential test, which holds the frozen HeadTracking.ini reader
# to v1.4.0's and the migration into CameraUnlock.ini to the frozen reader, core's canonical
# config lint over every file that test migrated, and the check that the frozen files keep their
# recorded hashes. Needs no game install.
test = { cmd = "ctest --test-dir build -C Release --output-on-failure", depends-on = ["build-release"] }

# Rewrites HeadTracking.ini, the committed config and the table's fresh render (the
# CameraUnlock.ini a first launch creates), after a change to a row, a comment or a default, then
# the config descriptor's per_game in launcher-manifest.json from it. The config tests fail until
# it has run.
render-config = { cmd = "build/Release/dl2ht_config_tests.exe --render-config HeadTracking.ini && node cameraunlock-core/scripts/encode-seed.mjs", depends-on = ["build-release"] }

# Package release ZIP. Depends on test, so no ZIP is built from a tree whose config tests fail.
package = { cmd = "powershell -ExecutionPolicy Bypass -File scripts/package-release.ps1", depends-on = ["build-release", "test"] }

# Automated release workflow
release = { cmd = "powershell -ExecutionPolicy Bypass -File scripts/release.ps1" }

# Core's own validators, run by hand. validate-manifest reads the newest release/*-installer.zip,
# so run it after package; until a release bumps the version to the config descriptor's
# canonical_since it reports that the ZIP names an earlier version.
validate-manifest = { cmd = "node cameraunlock-core/scripts/validate-manifest.mjs" }
validate-notices = { cmd = "node cameraunlock-core/scripts/validate-notices.mjs" }

# Preview changelog for next release
show-changelog = { cmd = "powershell -ExecutionPolicy Bypass -File scripts/show-changelog.ps1" }

# Revert release operations
unrelease = { cmd = "powershell -ExecutionPolicy Bypass -File scripts/unrelease.ps1" }

# Build + deploy release to game directory
install = { cmd = "powershell -ExecutionPolicy Bypass -File scripts/deploy.ps1 Release", depends-on = ["build-release"] }

# Remove HeadTracking mod only (keeps modding framework)
uninstall = "scripts\\uninstall.cmd"

# Test UDP receiver with simulated OpenTrack data
test-udp = { cmd = "python scripts/test-udp-receiver.py --test && python scripts/test-udp-receiver.py --yaw 45 --pitch 30 --roll 15" }

# Send continuous simulated tracking data for live testing
test-udp-continuous = { cmd = "python scripts/test-udp-receiver.py --continuous --rate 120" }

[dependencies]
# CMake and Visual Studio come from the system. Node runs the canonical config lint under `test`,
# render-config's encode-seed step and packaging's config descriptor check (Copy-SharedBundle),
# pinned to the release core's own lock resolves.
nodejs = "==26.5.0"
